// TODO: call the FastAPI backend's POST /recommend with the user's
// seed color(s) and render the returned suggestions as swatches.
// Fetch base URL should be configurable (local dev vs. deployed API).
